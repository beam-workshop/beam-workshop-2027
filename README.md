# Mind, Body and Soul Workshop Website (v9)

Verified correction:

- every speaker and organizer portrait is exactly **132 × 132 px**;
- each portrait is clipped to a circle;
- `object-fit: contain` preserves the full face instead of cropping it;
- `object-position: 50% 50%` centers the image;
- responsive breakpoints prevent organizer circles from shrinking;
- the standalone HTML embeds the entire stylesheet and script, so the preview visibly updates even when opened by itself.


## v10 update
The hero ribbon now reads **Proposed ICLR2027 Workshop**.
